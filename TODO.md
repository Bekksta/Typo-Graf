# TODO

## HIGH RISK

Правки, которые могут сломать поведение. Перед раскаткой в стабильный релиз — тестовый/beta-канал.

- **Math слишком агрессивен на `<буква>-<буква>`.** `iPhone 12-pro` → `iPhone 12 − pro`, `Wi-Fi`, `e-mail`, `re-init` страдают аналогично. Ограничить math явными математическими контекстами или блоками `$...$`.

- **Составные единицы с `/`.** `Мбит/с` → `Мбит / с`, аналогично `км/ч`, `м/с`, `кг/м²`, `об/мин`, `кадр/с`. Список устойчивых составных единиц + WJ-обёртка вокруг `/`.

- **Версии ломаются как numeric range.** `1.2-3 beta`, `0.10-rc1`, `v2.0.0-alpha` ловятся `NUM_RANGE_RE` и получают en-dash. Маскировать `\d+\.\d+(?:\.\d+)*(?:-\w+)?` как opaque-токен.

---

## НА ПОДУМАТЬ

- **NBSP перед композитными единицами с непривычным префиксом.** `Мбит/с` и `км/ч` получают NBSP между числом и единицей (префикс в `COMMON_UNITS_RE`), но `об/мин`, `кадр/с`, `руб/шт` — нет (префиксов нет в списке). Сейчас `/` уже защищён WJ от math-правила, но NBSP не вставляется. Варианты: (1) добавить `об|кадр|руб|...` в `COMMON_UNITS_RE` — риск ложных матчей с omonyms (`около 3 об` могло бы стать `около 3<NBSP>об`), (2) ввести отдельное правило «число + composite unit из словаря» с использованием `COMPOSITE_UNITS` напрямую. Низкий приоритет — типографика композитов уже не ломается, NBSP — это «было бы лучше».


Идеи без чёткого приоритета. Не входят в ближайшие PR.

- **Локали `fr-CA`, `pt-PT`, `es-AR`.** Различия в кавычках (smart `"…"` vs guillemets `«…»`). Маркеры (`courriel`, `telemóvel`, `vos/che`) могут появляться в текстах не той локали — ложный детект меняет кавычки на чужие. Внедрять через явный параметр локали или с порогом ≥2 strong markers.

- **Диалоги с авто-расстановкой тире-реплик.** Визуально неотличимо от буллет-списков. UI-флаг «диалог» или эвристика по количеству строк с тире.

- **Preview / dry-run режим.** Показывать diff правок перед применением. Требует UI. При том, что `⌘Z` откатывает весь прогон одной кнопкой — это уже дешёвый «try-and-revert preview».

- **JSDoc-пасс по экспортам.** Точечно добавлять JSDoc там, где WHY не очевиден из кода/комментариев. Полный пасс не нужен — code-base уже хорошо прокомментирован.

- **Реструктуризация по папкам.** Выделить, например, `src/rules/quotes/` под все `place*Quotes` и `src/rules/numbers/` под группировки тысяч. Делать когда rules-файлы вырастут заметно.
