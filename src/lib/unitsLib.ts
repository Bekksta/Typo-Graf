// Курируемый список составных единиц со слэшем (`Мбит/с`, `км/ч`, `руб/шт`).
// Используется в `maskSensitive` ([src/text/mask.ts]) как opaque-токен, чтобы
// math-правило не вставляло пробелы вокруг `/` (`Мбит/с → Мбит / с` — баг).
//
// Маска срабатывает ТОЛЬКО после числа (lookbehind `(?<=\d[\s ]*)`) —
// иначе ловили бы фразы «скорость Мбит/с» вне числового контекста.
//
// Сортировка по длине DESC для greedy-альтернации в regex: «Мбит/сек» должен
// матчиться раньше, чем префикс «Мбит/с».
export const COMPOSITE_UNITS = [
  // Передача данных (кириллица + латиница)
  "Мбит/сек", "Кбит/сек", "Гбит/сек",
  "Мбит/с", "Кбит/с", "Гбит/с",
  "Mbit/s", "Kbit/s", "Gbit/s",
  // Скорость движения
  "км/час", "км/ч", "м/с",
  "km/h", "m/s",
  // Расход
  "л/100км", "мл/ч", "л/ч",
  // Плотность
  "кг/м²", "кг/м³", "г/см³", "г/мл",
  // Частота / FPS
  "кадр/сек", "кадр/с", "об/мин",
  // Финансы (с цифрой слева: «100 руб/шт», «5 $/мес»)
  "руб/мес", "руб/час", "руб/шт",
  "$/мес", "$/час", "$/шт",
  "₽/мес", "₽/шт", "€/мес",
];
